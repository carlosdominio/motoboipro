name: Build Android APK (Motoboy App)

on:
  workflow_dispatch: # Permite rodar manualmente com um clique

jobs:
  build:
    name: Compilar APK Android (Motoboy)
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar o repositório
        uses: actions/checkout@v3

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: ☕ Configurar Java (JDK 17)
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 📦 Instalar dependências e Sincronizar Capacitor
        working-directory: ./motoboy-app-nativo
        run: |
          npm install
          if [ ! -d "android" ]; then
            npx cap add android
          fi
          npx cap sync android

      - name: 🔨 Compilar o APK (Android Studio Cloud)
        working-directory: ./motoboy-app-nativo/android
        run: ./gradlew assembleDebug

      - name: 🚀 Disponibilizar o APK para Download
        uses: actions/upload-artifact@v3
        with:
          name: MotoboyExpress-Nativo-Android
          path: motoboy-app-nativo/android/app/build/outputs/apk/debug/app-debug.apk
